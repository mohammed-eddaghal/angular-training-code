import { PropreDirectiveDirective } from './propre-directive.directive';

describe('PropreDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PropreDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
